<!DOCTYPE html>
<html>
<head>
  <title>Word Selection</title>
  <style>
    body {
      font-family: 'Roboto', sans-serif;
      padding: 20px;
      background-color: #f5f5f5;
    }
    h1 {
      color: #333;
      text-align: center;
      margin-bottom: 25px;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin: 20px 0;
      box-shadow: 0 2px 3px rgba(0,0,0,0.1);
      background-color: white;
    }
    th, td {
      padding: 12px 15px;
      text-align: left;
      border-bottom: 1px solid #ddd;
    }
    th {
      background-color: #f8f9fa;
      font-weight: 500;
      border-bottom: 2px solid #ddd;
    }
    tr:hover {
      background-color: #f9f9f9;
    }
    .row-checkbox {
      transform: scale(1.2);
      margin: 0;
      cursor: pointer;
    }
    .btn {
      padding: 10px 20px;
      background-color: #4CAF50;
      color: white;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      font-size: 16px;
      display: block;
      margin: 20px auto;
      transition: background-color 0.3s;
    }
    .btn:hover {
      background-color: #45a049;
    }
  </style>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500&display=swap" rel="stylesheet">
</head>
<body>
  <h1>Select those words you would like to exclude.</h1>
  
  <%= form_with url: selected_words_path, method: :post do |form| %>
    <table>
      <thead>
        <tr>
          <th>Select</th>
          <th>Word</th>
          <th>Transcription</th>
          <th>id words</th>
          <th>Example</th>
        </tr>
      </thead>
      <tbody>
        <% @words_array.each do |item| %>
          <tr>
            <td>
              <%= check_box_tag "selected_words[]", item.id, false, class: "row-checkbox" %>
            </td>
            <td><%= item.foreign_word %></td>
            <td><%= item.transcription %></td>
            <td><%= item.id words %></td>
            <td><%= item.example %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
    
    <%= form.submit "Обработать выбранные записи", class: "btn" %>
  <% end %>
</body>
</html>